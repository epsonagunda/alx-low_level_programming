#include "main.h"
/**
 * _isupper - program to check for uppercase
 * @c: variable text
 * Return: 0 (pass)
 */

int _isupper(int c)
{
if (c >= 'A' && c <= 'Z')
{
return (1);
}
else
{
return (0);
}
}
