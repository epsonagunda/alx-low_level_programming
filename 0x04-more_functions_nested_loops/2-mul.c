#include "main.h"
/**
 * mul - function to calculate the product of two numbers
 * @a:  first number
 * @b:  second number
 * Return: 0 (pass)
 */
int mul(int a, int b)
{
int mul;
mul = a * b;
return (mul);
}
