#include "main.h"
/**
 * _sqrt_recursion - function to return a natural sqrt of a number
 * @n: number
 * Return: 0 (pass)
 */
int _sqrt_recursion(int n)
{
if (b * b == a)
return (b);
else if (b * b > a)
return (-1);
return (sqrt2(a, b + 1));
}
{
return (sqrt2(n, 1));
}
